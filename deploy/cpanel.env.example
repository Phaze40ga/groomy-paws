#############################################
# Groomy Paws - cPanel Deployment Config
# Copy this file to deploy/cpanel.env and
# update the values before running setup.
#############################################

# ---- Domain ----
APP_DOMAIN=app.example.com            # Your public domain (serves SPA + proxies /api)

# ---- Paths (under your cPanel account) ----
WEB_ROOT=${HOME}/public_html/groomy-paws   # Where the repo lives on Hostinger/cPanel
# If you uploaded the repo elsewhere, the script will rsync it to WEB_ROOT

# ---- Backend ----
PORT=3001                             # Express listen port used by Passenger
JWT_SECRET=please-change-me           # REQUIRED. Long random string.

# ---- Database (MySQL) ----
# NOTE: On cPanel, DB and user names usually include your account prefix,
# e.g., user_dbname and user_dbuser. Use the exact names from MySQL® Databases.
DB_HOST=localhost
DB_PORT=3306
DB_NAME=user_dbname
DB_USER=user_dbuser
DB_PASSWORD=change-me-db-password   # REQUIRED. Avoid quotes or spaces.

# ---- Frontend Build ----
# Defaults to https://APP_DOMAIN/api if omitted
VITE_API_URL=

